import Section from "@/components/ui/Section";
import CustomTitle from "@/components/utils/CustomTitle";
import React from "react";

const TripTable = () => {
  return (
    <Section>
      <CustomTitle title={"Viajes"}></CustomTitle>
    </Section>
  );
};

export default TripTable;
