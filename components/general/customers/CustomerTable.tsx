import Section from "@/components/ui/Section";
import CustomTitle from "@/components/utils/CustomTitle";
import React from "react";

const CustomerTable = () => {
  return (
    <Section>
      <CustomTitle title={"Clientes"}></CustomTitle>
    </Section>
  );
};

export default CustomerTable;
