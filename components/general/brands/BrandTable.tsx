import Section from "@/components/ui/Section";
import CustomTitle from "@/components/utils/CustomTitle";
import React from "react";

const BrandTable = () => {
  return (
    <Section>
      <CustomTitle title={"Marcas"}></CustomTitle>
    </Section>
  );
};

export default BrandTable;
