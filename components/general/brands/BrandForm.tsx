import Section from "@/components/ui/Section";
import CustomTitle from "@/components/utils/CustomTitle";
import React from "react";

const BrandForm = () => {
  return (
    <Section>
      <CustomTitle title={"Registrar marca"}></CustomTitle>
    </Section>
  );
};

export default BrandForm;
