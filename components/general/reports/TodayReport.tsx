import Section from "@/components/ui/Section";
import CustomTitle from "@/components/utils/CustomTitle";
import React from "react";

const TodayReport = () => {
  return (
    <Section>
      <CustomTitle title={"Reporte del día"}></CustomTitle>
    </Section>
  );
};

export default TodayReport;
