import Section from "@/components/ui/Section";
import CustomTitle from "@/components/utils/CustomTitle";
import React from "react";

const CustomerForm = () => {
  return (
    <Section>
      <CustomTitle title={"Registrar cliente"}></CustomTitle>
    </Section>
  );
};

export default CustomerForm;
