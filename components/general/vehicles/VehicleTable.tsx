import Section from "@/components/ui/Section";
import CustomTitle from "@/components/utils/CustomTitle";
import React from "react";

const VehicleTable = () => {
  return (
    <Section>
      <CustomTitle title={"Vehículos"}></CustomTitle>
    </Section>
  );
};

export default VehicleTable;
